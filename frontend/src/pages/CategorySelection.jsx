import React from 'react';
import MagicBento from '../components/MagicBento';

const CategorySelection = () => {
  return (
    <div style={{ width: '100vw', minHeight: '100vh', backgroundColor: '#000', display: 'flex', justifyContent: 'center', alignItems: 'center', padding: '2rem' }}>
      <MagicBento />
    </div>
  );
};

export default CategorySelection;
